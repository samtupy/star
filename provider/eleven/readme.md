# eleven

This provider wraps the ElevenLabs API. You'll need to run it with the --configure command line argument to provide your API key.

If you want to run the provider from source, make sure to run `pip install elevenlabs` to procure needed requirements.
